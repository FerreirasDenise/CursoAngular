var minhaVar = 'minha variavel';

function minhaFunc(x, y){
    return x + y;
}

let num = 2;
const PI = 3.14;

var numeros = [1, 2, 3];
numeros.map(function(valor){
    valor * 2;
})

numeros.map(valor => valor * 2) //ES2015

class Mathematica{
    soma (x, y){
        return x + y;
    }
}

var n1: any = 'hdhakdhka';
n1 = 2;